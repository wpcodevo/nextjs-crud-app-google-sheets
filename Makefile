install-dep:
	pnpm add @tanstack/react-query @tanstack/react-query-devtools
	pnpm add axios
	pnpm add zod react-hook-form @hookform/resolvers react-toastify date-fns tailwind-merge
	pnpm add nprogress
	pnpm add @tanstack/react-query

	pnpm add -D @types/nprogress